getgenv().VO_CONFIG = {
    -- === HUB / AUTH ===
    HubKey = "vezlSS_S-3VtvaC9Moz9RuQQ4Xf5qhV7VAQT-K_1puQ",
    DeviceName = "",

    -- === MAIN FARM (choose one mode) ===
    PotFarm = true,
    EggFarm = false,
    KeepEggFarm = false,
    EggName = {"Egg Name"},  -- Priority order: first egg tried, then second, etc.
    PrioritizePet = "2D Kitty",

    -- === EVENT FARM ===
    AutoBuyCandyChisel = false,
    AutoBuyMochiMallet = false,

    -- === PET PEN ===
    PetPen = false,
    CustomPenEggs = {"Egg Name"},
    CustomPenPets = {},
    PrioritizePetPenTypes = {},  -- "Egg", "Normal", "Neon" (empty = all)

    -- === PET RELEASER ===
    PetReleaser =  true,
    ReleasePets = {},       -- Whitelist: names to release (empty = all)
    ExcludeReleasePets = {"Mochi Meow", "Frostbite Bear", "Dark Choccybunny"}, -- Blacklist: names to never release (overrides ReleasePets)
    ReleaseTypes = {},      -- "Mega", "Neon", "Normal" (empty = all)

    -- === AGE PETS ===
    AgePets = true,
    AgePetsNames = {"Mochi Meow"},
    AgePetsTypes = {"Normal"},  -- "ALL", "Neon", "ALL"

    -- === AUTO FUSE ===
    AutoFuse = true,

    -- === BUY PETS ===
    BuyPets = false,
    BuyPetName = {"Pet Name", "Pet Name 2"},  -- Loops in order, buys all of first pet then moves to next

    -- === BOXES ===
    BuyBoxes = false,
    BoxName = "Choccybunny Box",   -- Name of the box to buy/open
    OpenBoxes = true,

    -- === LURE / BAIT ===
    BaitName = "Bait Name",

    -- === DISCORD WEBHOOK ===
    WebhookEnabled = true,
    WebhookURL = "https://discord.com/api/webhooks/1484296571238617149/bmfWBPShfN3BKz2X9xHPQ9i0djy63ZsDF0UgtkHNnVm8Z9p_TNr2WSSyPKmPOWFFZ84q",
    WebhookPets = {},  -- Pet names to send (empty = all)

    ExtraOpti = false  -- Experimental; may cause issues
}
loadstring(game:HttpGet("https://raw.githubusercontent.com/voltrex2/VoHub/refs/heads/main/FARM"))()
